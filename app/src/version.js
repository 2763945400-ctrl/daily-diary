// 应用版本号。纯展示，只出现在设置页底部，不参与存储、不进备份文件。
//
// ⚠️ 别跟备份文件里的 "version": 2 搞混 —— 那个是**数据格式版本**，
//    是新旧两版互通的桥，规格第四节列为不许动，永远是 2。这里是**应用版本**。
//
// 从 2.0.0 起（2026-09-05 用户拍板）：整份实现换掉了，且迁移期新旧两个 App
// 会同时装在手机上（appId 不同，笔记第八节），版本号是一眼分清哪个是哪个的东西。
export const APP_VERSION = '2.0.0';
