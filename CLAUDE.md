# 项目规则 · 每日一记

给在这个仓库里工作的 AI 会话看的。与用户的全局规则合并使用。
背景和完整历史见 `docs/协作开发笔记.md`（本地文件，不进仓库）和 `改造规格-*.md`。

---

## 1. ⚠️ 这个仓库里有两套代码，只有一套是活的

| 目录 | 规则 |
|---|---|
| **`app/`** | ✅ 当前版本，改功能改这里 |
| `lib/` `ios/` `android/` `test/` `pubspec.yaml` `assets/` `web/` | ❄️ 旧的 Flutter 版，**冻结，一个字都不许改** |
| `.github/workflows/ios-build.yml` | ❄️ **冻结**，是 iOS 打包的退路 |
| `app/design/` | 🔒 **只读**。设计稿和设计 token，**要改先问用户**（历史上只授权过两次，都是新增 token） |

Flutter 那份是**回滚保险**，在新版彻底取代它之前不删任何东西。
看到 `lib/` 里的 Dart 代码不要以为那是当前实现 —— 它已经不上线了。

## 2. 这些地方不许另起炉灶

| 东西 | 规则 |
|---|---|
| **日期计算** | `app/src/day.js` 的 `logicalKey()` 是**全应用唯一真源**（凌晨 2 点日界）。**严禁在别处另写日期计算** —— Flutter 版因为这个逻辑散在 4 处出过 bug |
| **导出 JSON 的字段名和结构** | `app/src/backup.js`。这是新旧两版互通的**唯一桥梁**，一个字母都不能改 |
| **IndexedDB 结构** | 库名 `diary`，store 为 `entries`（主键 `yyyy-MM-dd`）/ `notes`（主键=毫秒时间戳字符串）/ `settings`。时间一律存毫秒整数，照片存 Blob 不存 base64 |

## 3. 已经决策掉的事，别"顺手"加回来

- **不做任何后端、账号、云同步。** 数据不出设备是这个应用的核心优势
- **不做访问统计 / 点击统计 / 任何埋点。** 2026-08-01 明确决策。日记内容尤其不许作为"事件参数"外传
- **不做连续记录（streak）。** 2026-08-04 明确删除 —— 断一天就成心理负担，与"想记就记"的产品定位相反。旧版回顾页有，新版**故意没有**
- **不做补记功能**（用户已否决）
- **网页版不做提醒功能**（浏览器给不了本地定时通知，别尝试）

## 4. 体积预算：gzip 150 KB

当前首次访问约 21 KB。**整个重写的唯一目的就是把体积压下来**（Flutter 版 4.4 MB 在波动链路下加载不稳）。

- **加依赖前先算体积，并先问用户。** `package.json` 从项目第 1 步至今只有 Capacitor，没有构建工具以外的运行时依赖
- 不打包字体（用系统字体）、不打包音频（钟声是 Web Audio 合成的）
- 需要某个库的一小部分功能时，优先自己写几十行

## 5. Service Worker（`app/src/sw.js` + `vite.config.js` 的插件）

- **绝对不许调 `self.skipWaiting()`** —— 会在用户正写日记时当场顶掉旧版本，打断输入
- 缓存名带版本戳，版本戳按**产物内容**算不按文件名算（`index.html` 文件名不带哈希）
- `install` 用 `cache.addAll()` 全量预缓存，任一文件失败则整次失败，不留半份缓存
- 构建插件必须 `enforce: 'post'`，否则 `index.html` 进不了预缓存清单（Vite 内部插件更晚才把它塞进 bundle）。插件里的两条断言是防这个的，**别删**
- ⚠️ **子路径部署时，站点根目录的隐式请求（如 `/favicon.ico`）在 SW 作用域之外，拦不到**。要加根目录资源时注意

## 6. 验收怎么做

- **改完跑 `cd app && npm test`**（当前 67 项）和 `npm run build`。CI 里测试不过就不发布
- **控制台 0 报错**是硬性验收标准
- ⚠️ **内置浏览器面板注册不了 Service Worker**（页面里 `fetch('./sw.js')` 是 200，但 `register()` 报 "fetching the script" 失败）。**验 SW 只能用真 Chrome**
- 验离线的最狠办法：`npm run build` → 静态服务器跑 `dist/` → **把服务器杀掉**再刷新。比断网更彻底
- 本地静态服务器**必须多线程**，单线程下 SW 脚本抓取会失败
- 隐藏标签页不跑渲染管线，`getComputedStyle` 读带 `transition` 的属性会读到起始值 —— 是验收环境的假象，不是 bug

## 7. 环境（Windows 11）

- 终端是 **PowerShell 5.1**：**不认 `&&`**。给用户的命令一条一条给，别串联
- **Windows 的 python 认不了 Git Bash 的 `/c/...` 路径**，脚本里写 `C:/...`
- PowerShell 5.1 默认 GBK 输出，python 脚本打印非 ASCII 要 `PYTHONIOENCODING=utf-8`
- PowerShell 5.1 的 `ConvertFrom-Json` 撑不住大文件
- dev server 固定 8686，重启注意 errno 10048（端口占用）

## 8. 交付与提交

- **推到 `master` 即自动部署上线**（约一分钟）。这不是演练环境
- 每步做完汇报：改了哪些文件、gzip 体积、控制台有无报错、**特意没做什么**
- **提交前把内容给用户看**；`git push` 由用户决定
- 版本号写在 `app/src/version.js`，归用户管
